<Window x:Class="Draw2D.PathDemo.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:Controls="clr-namespace:Draw2D.Wpf.Controls"
        Title="Draw2D.PathDemo" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        UseLayoutRounding="True" SnapsToDevicePixels="True" TextOptions.TextFormattingMode="Display">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" ItemsSource="{Binding Tools}" SelectedItem="{Binding CurrentTool}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Margin="2"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Controls:ShapesContainerInputView x:Name="InputView" Grid.Column="1" Background="Transparent" ClipToBounds="True">
            <Controls:ShapesContainerRenderView x:Name="RendererView" Width="{Binding CurrentContainer.Width}" Height="{Binding CurrentContainer.Height}" Background="LightSlateGray" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </Controls:ShapesContainerInputView>
    </Grid>
</Window>
